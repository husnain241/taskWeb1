@model Post_Crud.Models.Post

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit Post</h1>

<h4>Post</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Post_Id" />
            <div class="form-group">
                <label asp-for="Post_Name" class="control-label"></label>
                <input asp-for="Post_Name" class="form-control" />
                <span asp-validation-for="Post_Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Post_Description" class="control-label"></label>
                <input asp-for="Post_Description" class="form-control" />
                <span asp-validation-for="Post_Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
